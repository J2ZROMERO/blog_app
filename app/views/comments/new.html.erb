<h1>New comment</h1>

<%= form_with(model: @comment,url: user_posts_path(:user_id => 1), method: :post, local: true)  do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :text %>
  <%= form.text_area :text %>
  
  <%= form.submit %>

<% end %>
<h1>Nuevo artículo</h1>

<%= form_with(model: @post, local: true, url:users_path) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :text %>
  <%= form.text_area :text %>
  
  <%= form.submit %>
<%= @s %>

<% end %>